server:
  port: 8081

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/saga?currentSchema=order_db
    username: saga
    password: saga
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.default_schema: order_db
      hibernate.jdbc.time_zone: UTC
    open-in-view: false
  flyway:
    enabled: true
    default-schema: order_db
    schemas: order_db
    locations: classpath:db/migration
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: order-service
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "*"
    producer:
      acks: all
      properties:
        enable.idempotence: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      service: ${spring.application.name}

saga:
  outbox:
    poll-interval-ms: 500
    batch-size: 50
    # Bound the synchronous Kafka send so a hung broker never blocks the publisher thread.
    send-timeout-ms: 5000
    # After this many consecutive failures the row is moved to the parking lot
    # (parked_at set, polling index skips it) and an ERROR log fires for operator alerting.
    max-attempts: 10
  # Stuck-saga detector: any non-terminal saga whose updated_at is older than
  # timeout-seconds will be forced forward (FAILED or COMPENSATING) by
  # SagaTimeoutScanner on its next scan tick.
  timeout-seconds: 120
  timeout-scan-seconds: 30

logging:
  level:
    com.quanla.sagademo: DEBUG
    org.hibernate.SQL: WARN